// Give an algorithm for finding the penultimate (second to last) node in a
// singly linked list where the last element is indicated by a bull next link.
//
//
// Algorithm:

// 1. If the list contains fewer than two nodes, report that no penultimate node exists.
// 2. Start at the head node.
// 3. Traverse the list until the current node's next node is the last node
//    (i.e., current->next->next == nullptr).
// 4. Return the current node.

int main() { return 0; }
